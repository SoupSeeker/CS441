<odoo>
  <data>
    <!-- explicit list view definition -->

    <record model="ir.ui.view" id="beta_mod.list">
      <field name="name">beta_mod list</field>
      <field name="model">beta_mod.beta_mod</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>


    <!-- actions opening views on models -->

    <record model="ir.actions.act_window" id="beta_mod.action_window">
      <field name="name">beta_mod window</field>
      <field name="res_model">beta_mod.beta_mod</field>
      <field name="view_mode">tree,form</field>
    </record>


    <!-- server action to the one above -->

    <record model="ir.actions.server" id="beta_mod.action_server">
      <field name="name">beta_mod server</field>
      <field name="model_id" ref="model_beta_mod_beta_mod"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>


    <!-- Top menu item -->

    <menuitem name="beta_mod" id="beta_mod.menu_root"/>

    <!-- menu categories -->

    <menuitem name="Menu 1" id="beta_mod.menu_1" parent="beta_mod.menu_root"/>
    <menuitem name="Menu 2" id="beta_mod.menu_2" parent="beta_mod.menu_root"/>

    <!-- actions -->

    <menuitem name="List" id="beta_mod.menu_1_list" parent="beta_mod.menu_1"
              action="beta_mod.action_window"/>
    <menuitem name="Server to list" id="beta_mod" parent="beta_mod.menu_2"
              action="beta_mod.action_server"/>

  </data>
</odoo>